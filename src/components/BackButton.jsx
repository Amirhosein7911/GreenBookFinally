// import { useNavigate, useLocation } from "react-router-dom";

// function BackButton() {
//   const navigate = useNavigate();
//   const location = useLocation();

//   if (location.pathname === "/") return null;

//   return (
//     <button
//       className="fixed top-5 left-4 z-50 px-7 py-2 bg-teal-700 text-white font-extrabold text-lg rounded-lg shadow-md hover:bg-teal-950 transition-colors flex items-center gap-2 select-none"
//       onClick={() => navigate(-1)}
//     >
//       بازگشت
//     </button>
//   );
// }

// export default BackButton;
